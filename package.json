{
  "name": "loewenherz-app",
  "private": true,
  "description": "Löwenherz PWA — Web ohne Build-Step; package.json existiert nur für das Capacitor-iOS-Fundament",
  "scripts": {
    "build:ios": "node scripts/build-ios.mjs && cap sync ios"
  },
  "dependencies": {
    "@capacitor/core": "^8.5.0",
    "@capacitor/ios": "^8.5.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^8.5.0"
  }
}
